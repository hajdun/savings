/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { IncomeAdditionPageComponent } from './income-addition-page.component';

describe('IncomeAdditionPageComponent', () => {
  let component: IncomeAdditionPageComponent;
  let fixture: ComponentFixture<IncomeAdditionPageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ IncomeAdditionPageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(IncomeAdditionPageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
