/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CostAdditionPageComponent } from './cost-addition-page.component';

describe('CostAdditionPageComponent', () => {
  let component: CostAdditionPageComponent;
  let fixture: ComponentFixture<CostAdditionPageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CostAdditionPageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CostAdditionPageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
