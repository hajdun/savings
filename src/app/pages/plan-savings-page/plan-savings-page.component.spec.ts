/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PlanSavingsPageComponent } from './plan-savings-page.component';

describe('PlanSavingsPageComponent', () => {
  let component: PlanSavingsPageComponent;
  let fixture: ComponentFixture<PlanSavingsPageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PlanSavingsPageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PlanSavingsPageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
