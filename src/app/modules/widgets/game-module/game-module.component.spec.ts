/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { GameModuleComponent } from './game-module.component';

describe('GameModuleComponent', () => {
  let component: GameModuleComponent;
  let fixture: ComponentFixture<GameModuleComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ GameModuleComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(GameModuleComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
