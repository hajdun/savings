/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CostSumComponent } from './cost-sum.component';

describe('CostSumComponent', () => {
  let component: CostSumComponent;
  let fixture: ComponentFixture<CostSumComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CostSumComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CostSumComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
