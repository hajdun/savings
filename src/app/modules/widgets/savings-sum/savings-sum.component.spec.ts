/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SavingsSumComponent } from './savings-sum.component';

describe('SavingsSumComponent', () => {
  let component: SavingsSumComponent;
  let fixture: ComponentFixture<SavingsSumComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SavingsSumComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SavingsSumComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
