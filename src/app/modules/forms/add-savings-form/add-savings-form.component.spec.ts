/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AddSavingsFormComponent } from './add-savings-form.component';

describe('AddSavingsFormComponent', () => {
  let component: AddSavingsFormComponent;
  let fixture: ComponentFixture<AddSavingsFormComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AddSavingsFormComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AddSavingsFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
