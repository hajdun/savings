/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AddSettingsFormComponent } from './add-settings-form.component';

describe('AddSettingsFormComponent', () => {
  let component: AddSettingsFormComponent;
  let fixture: ComponentFixture<AddSettingsFormComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AddSettingsFormComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AddSettingsFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
